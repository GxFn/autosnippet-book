Title at top in bold Chinese: "继承体系 vs 统一实体"

Two columns separated by a bold vertical "VS" divider in the center. Pale pink (#FADBD8) tinting the left "rejected" side and pale blue (#A8D4F0) tinting the right "chosen" side.

LEFT COLUMN (labeled "❌ 面向对象继承"):
- A tree diagram at top: root box "BaseKnowledge" with 4 child boxes branching down: "Pattern", "Decision", "Convention", "Practice"
- Below the tree, 3 problem callout boxes with warning icons (⚠):
  1. "搜索需要 UNION 4 张表" with a sketch of 4 small tables being merged
  2. "生命周期代码 ×4 复制" with a sketch of 4 identical circular arrows
  3. "分类边界模糊" with a sketch of overlapping dotted circles and a question mark

RIGHT COLUMN (labeled "✅ 统一实体 + 分类标签"):
- A single large rounded rectangle "KnowledgeEntry" at center
- From its left side, an arrow labeled "行为维度 kind" branches to 3 small boxes: "rule → Guard 检查", "pattern → 代码模板", "fact → 关系描述"
- From its right side, an arrow labeled "语义维度 knowledgeType" branches to 5 small labels: "code-pattern", "architecture", "best-practice", "conventions", "project-profile"
- Below the entity, a single horizontal flow: "pending → staging → active → deprecated" (shared lifecycle state machine), drawn as circles connected by arrows
- 3 benefit checkmarks (✓): "一张表", "一套 API", "一个状态机"

CENTER DIVIDER: Large bold "VS" text in a circle, with decorative hand-drawn lightning bolts around it
